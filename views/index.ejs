<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Form Example</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f2f2f2; margin: 50px; }
    form { background: white; padding: 20px; border-radius: 10px; width: 300px; }
    input, button { width: 100%; margin-top: 10px; padding: 8px; }
  </style>
</head>
<body>
  <h2>User Input Form</h2>
  <form action="/submit" method="POST">
    <label>Name:</label>
    <input type="text" name="username" required>

    <label>Email:</label>
    <input type="email" name="email" required>

    <button type="submit">Submit</button>
  </form>

  <% if (user) { 
    %>
    <h3>Submitted Data:</h3>
    <p><strong>Name:</strong> <%= user.username %></p>
    <p><strong>Email:</strong> <%= user.email %></p>
   <% 
    } %>
</body>
</html>
